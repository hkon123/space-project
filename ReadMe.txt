To start the program run initialise.py
To make custom universes use the template.txt file (There can only be one main sun, and the sun has to be first in the list, but it can have whatever properties) add as many planets and moons as you want
to make custom satelite launches use the ship.txt file as a template but do not overwrite the file but create a new one instead